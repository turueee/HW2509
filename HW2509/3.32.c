/*
#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
  printf("((x<=-2) && (y>=1))\n");
  printf("((y>=-2) && (y<=1.5))\n");
  printf("((y<=4) && (x>=1) && (x<=2))\n");
  printf("((x>=1) && (y>=2) && (y<=4))\n");
  printf("(((y>=0) && (x>=2)) || ((y<=-1) && (x>=1)))\n");
  printf("(((x>=2) && (y>=1)) || ((y<=-1.5) && (x>=2)))\n");
  printf("((x>=1) && (x<=3) && (y>=-2) && (y<=-1))\n");
  printf("((x>=2) || ((y>=0.5) && (y<=1.5)))\n");
}
*/